import React from "react"

const ContentSketch = ({ data }) => {
  console.log("ContentSketch", data)
  return (
    <div>
      <h2>Content Sketch</h2>
    </div>
  )
}

export default ContentSketch
